/// The [Environments] class defines constants for different environment names.
/// These constants are used to differentiate between local, staging, and production environments.
class Environments {
  static const String prod = 'prod';
  static const String stage = 'stage';
  static const String local = 'local';
}
